<?php
/**
 * Front page template
 *
 * All content zones are rendered by header.php's shell-grid via
 * the config's zones[] and content_mapping[]. This template
 * intentionally renders nothing extra — the zone system owns
 * the entire page layout.
 */
get_header();
get_footer();
